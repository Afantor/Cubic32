set(COMPONENT_SRCDIRS "src" "src/utility" "src/Fonts")
set(COMPONENT_ADD_INCLUDEDIRS "src")
list(APPEND COMPONENT_REQUIRES "arduino")

register_component()


set(DEFINITIONS "ESP32")

if (CONFIG_CUBIC32_BASIC OR CONFIG_CUBIC32_GRAY OR CONFIG_CUBIC32_FIRE)
    list(APPEND DEFINITIONS "CUBIC32")
endif (CONFIG_CUBIC32_BASIC OR CONFIG_CUBIC32_GRAY OR CONFIG_CUBIC32_FIRE)

if(CONFIG_CUBIC32_FIRE)
    list(APPEND DEFINITIONS "CUBIC32_FIRE")
endif(CONFIG_CUBIC32_FIRE)

target_compile_definitions(${COMPONENT_TARGET} PUBLIC ${DEFINITIONS})

